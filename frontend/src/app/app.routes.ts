import { Routes } from '@angular/router';
import { DesignKitComponent } from './pages/designkit/designkit.component';

export const routes: Routes = [
  { path: 'designkit', component: DesignKitComponent },
];
